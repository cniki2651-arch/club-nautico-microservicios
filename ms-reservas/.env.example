PORT=8085
NODE_ENV=production
CORS_ORIGIN=*

# Cadena de conexion a un cluster MongoDB Atlas (o cualquier MongoDB compatible).
# Pide la credencial real al resto del equipo, no la subas nunca a git.
MONGO_URI=mongodb+srv://USUARIO:PASSWORD@CLUSTER.mongodb.net/club_nautico_reservas?retryWrites=true&w=majority
